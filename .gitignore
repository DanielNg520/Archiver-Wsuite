# ── Secrets / credentials (belt-and-suspenders; these usually live in ~/.config) ──
.env
*.env
.env.*
!.env.example
*.session
*.session-journal
cookies/
*cookies*.txt
tiktok.txt

# ── Python build / install artifacts ──
build/
dist/
*.egg-info/
__pycache__/
*.py[cod]
*.egg
.eggs/

# ── pipx / venv (shouldn't be in repo, but just in case) ──
.venv/
venv/

# ── Self-contained config root (Linux): DB, config.toml, .env, sessions,
#    cookies, logs and locks all live here so the checkout carries its own
#    state. Never track any of it. ──
.config/

# ── Runtime state / databases / logs ──
*.db
*.db-wal
*.db-shm
*.session
*.log
pid
room_id_cache.json
locks/

# ── Media output (never commit downloaded media) ──
archiver_downloads/
recorder-output/
*.mp4
*.ts
*.jpg
*.jpeg
*.png
*.webp
*.mkv

# ── macOS / editor cruft ──
.DS_Store
*.swp
.idea/
.vscode/

# ── The path-collision artifacts you already deleted ──
mnt/

# ── Librarian is its OWN repo (github.com/DanielNg520/Librarian); never track it here ──
librarian/

# ── Streamer is its OWN repo (independent worker; archiver suite is reference only); never track it here ──
Streamer/
Autosplitter.zip

# ── Miki (Telegram archiver) is its OWN repo; this local-only "join" appends a
#    MIKI section to `ops watch` on THIS machine only — never track it here ──
miki_status.py
watch-miki.bat
